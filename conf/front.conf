# Front mode: try files first, then PHP (no directory fallback)
location __PATH__/ {
    root __INSTALL_DIR__/www/;
    index index.php index.html;
    try_files $uri $uri/ __PATH__/index.php$is_args$args;
}
